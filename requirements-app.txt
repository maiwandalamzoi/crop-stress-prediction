# Trimmed dependency set for deploying app_streamlit.py only (e.g. Streamlit
# Community Cloud). Excludes earthengine-api/requests, which the
# extraction/training scripts need but the app itself does not import.
# scikit-learn and xgboost are still required here even though the app
# doesn't import them directly -- joblib.load() needs them to unpickle the
# saved RandomForestClassifier / XGBClassifier objects.
#
# scikit-learn and xgboost are PINNED (==) to the exact versions used to
# train and pickle the committed models/*.joblib files -- unpickling across
# a scikit-learn minor-version bump is not guaranteed to work (confirmed via
# a real InconsistentVersionWarning when testing against a freshly resolved
# 1.9.0 vs. the 1.8.0 the models were trained with). If you retrain the
# models with a different version, update this pin to match.
pandas>=2.0
numpy>=1.26
scikit-learn==1.8.0
xgboost==3.2.0
matplotlib>=3.8
shap>=0.45
joblib>=1.3
streamlit>=1.30
folium>=0.15
streamlit-folium>=0.18
